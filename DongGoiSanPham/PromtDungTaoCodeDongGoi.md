Tôi cần thêm cơ chế cấu hình tạo clip như ConfigControls.tsx vào đầu aside cho các app khác, hãy viết tôi promt để mô tả cho các app khác hiểu cần chỉnh sửa gì, nếu app đó đã có config cho tỉ lệ, model, ... trong ConfigControls.tsx rồi thì bỏ đi mà chuyển sang chỉ dùng từ ConfigControls.tsx. Và trong promt có sẵn luôn chính xác code ConfigControls.tsx để app tạo ra file này với chính xác code như thế luôn cho đồng bộ